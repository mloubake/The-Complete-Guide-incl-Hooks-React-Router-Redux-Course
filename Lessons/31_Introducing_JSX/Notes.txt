This lesson introduces JSX and how it works behind the scene. Basically JSX can be written in 
a javascript way (with html) and will be transformed in a complex chunck of javascript code
(generated by the framework, that will run in the browser), so it's not worth to understand.